$(document).ready(function(){

    // Scores
    $('').on('input', function() {
        $('').text(''); 
    });
    
    // Event
    $('').on('input', function() {
        $('').text(''); 
    });

    // Club
    $('').on('input', function() {
        $('').text(''); 
    });

    // Student
    $('').on('input', function() {
        $('').text(''); 
    });

    // Account
    $('').on('input', function() {
        $('').text(''); 
    });
})