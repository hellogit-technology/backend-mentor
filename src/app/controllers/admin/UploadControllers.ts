import { Request, Response, NextFunction } from 'express';

class UploadControllers {
  poster(req: Request, res: Response, next: NextFunction) {
    try {
    } catch (error) {
      console.log(error);
    }
  }

  excelStudent(req: Request, res: Response, next: NextFunction) {
    try {
    } catch (error) {
      console.log(error);
    }
  }
}

export default new UploadControllers();
